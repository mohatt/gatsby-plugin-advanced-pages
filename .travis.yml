language: node_js
os:
  - linux
cache:
  yarn: true
  directories:
    - ~/.npm
node_js:
  - '8'
install:
  - yarn global add codecov
  - yarn
script:
  - npm run test:coverage
  - npm run build
matrix:
  allow_failures: []
  fast_finish: true
after_success:
  - yarn semantic-release
  - codecov
branches:
  except:
    - /^v\d+\.\d+\.\d+$/